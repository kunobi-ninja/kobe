Kobe has been installed in namespace {{ .Release.Namespace }}.

To verify the operator is running:

  kubectl get pods -n {{ .Release.Namespace }}

To check the health endpoint:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "kobe.fullname" . }} {{ .Values.service.port }}
  curl http://localhost:{{ .Values.service.port }}/healthz

To create a cluster pool:

  cat <<EOF | kubectl apply -f -
  apiVersion: kobe.kunobi.ninja/v1alpha1
  kind: ClusterPool
  metadata:
    name: my-pool
  spec:
    size: 3
    ttl: "2h"
    backend:
      type: k3s
    cluster:
      version: "v1.31.3+k3s1"
      servers: 1
  EOF
{{- if ne .Values.agentSandbox.mode "disabled" }}

Sandbox admission ledger:

  Namespace: {{ include "kobe.sandboxLedgerNamespace" . }}
  Object limit: {{ .Values.agentSandbox.reservationObjectLimit }} coordination Leases

The ledger namespace and its admission policy/quota are retained on Helm
uninstall. Do not delete them until every SandboxLease and reservation is
verified absent. The ledger namespace must not be shared with other controllers.
{{- end }}
